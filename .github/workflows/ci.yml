name: Build plugin (x64)

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Setup NuGet
        uses: nuget/setup-nuget@v2
        with:
          nuget-version: "6.x"

      - name: Restore packages
        run: nuget restore src\TeamCord.sln

      # 直接编译插件 csproj（会先编 TeamCord.Core）。Core 工程需含 Release|x64，否则 Platform=x64 时 OutputPath 为空会失败。
      - name: Build TeamCord.Plugin (Release x64)
        run: >
          msbuild.exe src\TeamCord.Plugin\TeamCord.Plugin.csproj
          /p:Configuration=Release
          /p:Platform=x64
          /p:PostBuildEvent=
          /v:m
          /m

      - name: Upload TeamCord.dll
        uses: actions/upload-artifact@v4
        with:
          name: TeamCord-plugin-x64-Release
          path: src\TeamCord.Plugin\bin\x64\Release\TeamCord.dll
          if-no-files-found: error
          retention-days: 14
